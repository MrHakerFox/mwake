This module is loadable under linux. It proved you with WAKE protocol.
It installs under name /dev/mwake.
It can be used without IOCTL requests as its default settings are:
1. Baudrate 38400, 8, N, 1.

